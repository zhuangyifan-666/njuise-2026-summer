# AI4SE 期末项目 B · 完成流程

> 本流程适用于“非 Harness 应用类项目”，与 `要求.md`、`AI4SE_Final_Project_B_应用类项目.md` 配合使用。
>
> 为控制项目风险，默认选择普通 Web 应用或带单轮 LLM 能力的应用，不加入“自主多轮决策 + 自主调用工具”的 Agent。若后续确实需要 Agent，必须额外补充自研主循环、工具分发、治理护栏和确定性测试。

## 总体门禁

项目按以下顺序推进：

`选题 → brainstorming → SPEC → PLAN → 陌生智能体冷启动 → 修订规约 → TDD 实现 → 两阶段评审 → CI/分发/部署 → 文档与提交审计`

在 `SPEC.md`、`PLAN.md` 完成并经过陌生智能体冷启动验证之前，不编写正式实现代码。

---

## 阶段 0：确认课程基础设施

### 要做的事

1. 向教师或助教确认代码托管平台究竟使用 NJU GitLab 还是 GitHub。
2. 确认是否必须同时提供 `.gitlab-ci.yml` 和 GitHub Actions。
3. 确认最终提交入口、截止时间以及线上服务需保持可访问到何时。

### 已确认的部署规则（助教补充说明）

作业 A 和作业 B 均可选择以下任一方案：

- **方案一：仅提供 CLI。** 不必额外开发 WebUI，但必须创建托管平台的 Release，并提交可访问的 Release 链接，例如 GitHub Releases 链接。
- **方案二：同时提供 CLI 和 WebUI。** 提交 CLI 分发产物，同时提供可供检查访问的 WebUI 链接；架构可参考同时提供 `llama-cli` 和 `llama-server` 的 llama.cpp。

因此 WebUI 不再视为所有项目的强制要求。项目必须在 `SPEC.md` 中明确选择方案一或方案二，并写出对应的验收标准。

### 其他暂定执行标准

在其他问题得到答复前按更严格标准执行：

- 提供 `.gitlab-ci.yml`，其中包含名称为 `unit-test` 的 job。
- 保留完整分支、commit、Merge Request / Pull Request 历史。
- 确保最后一次 CI/CD 执行通过。

### 完成标志

将教师答复和最终采用的规则记录到 `AGENT_LOG.md`。

---

## 阶段 1：确定选题和项目边界

### 选题必须满足

- 能在 30 秒内说明目标用户、痛点和价值。
- 至少包含 3 个职责清晰的功能模块。
- 不是简单聊天框、单页展示或现成 API 的薄封装。
- 核心功能可以用自动化测试验证。
- 能通过 CLI 或“CLI + WebUI”展示完整用户流程。
- 方案一能创建可下载、带版本号的 Release；方案二能提供 CLI 分发产物和可在线访问的 WebUI。
- 能在课程时间和个人能力范围内完成。

### 选题说明模板

> 为【目标用户】解决【具体问题】，通过【核心能力】将【当前成本或风险】降低到【可验证结果】。

### 本阶段产物

创建一页选题说明，至少包含：

- 目标用户与真实场景
- 现有解决方式的不足
- 3–5 个核心模块
- 一条端到端主流程
- 明确不做的功能
- 是否使用 LLM 或其他鉴权 API
- 初步技术栈、分发方式和部署方式

### Gate 1

只有在项目价值、三个核心模块和不做范围都明确后，才能进入规约设计。

---

## 阶段 2：准备 Superpowers 和过程证据

### 要做的事

1. 安装并验证 Superpowers。
2. 初始化代码仓库和基础分支策略。
3. 创建以下空白过程文档：
   - `SPEC.md`
   - `PLAN.md`
   - `SPEC_PROCESS.md`
   - `AGENT_LOG.md`
4. 在 `AGENT_LOG.md` 记录日期、使用的智能体、Superpowers 技能及关键 prompt。

### 注意

此阶段只准备工作流和文档，不创建业务实现。

---

## 阶段 3：使用 brainstorming 完成 `SPEC.md`

### Brainstorming 至少完成三轮关键迭代

1. **问题和用户轮**：确认谁在什么场景下遇到什么问题。
2. **功能和边界轮**：明确输入、行为、输出、异常和不做范围。
3. **架构和风险轮**：确认数据流、外部依赖、安全、部署和验收方式。

每轮都要在 `SPEC_PROCESS.md` 中保留：

- 智能体提出的关键问题
- 原设想存在的模糊点
- 采纳、拒绝或修改了什么建议
- 做出决定的理由

### `SPEC.md` 必备章节

1. 问题陈述和目标用户
2. 至少 5 个符合 INVEST 的用户故事
3. 按模块组织的功能规约
4. 非功能需求：性能、安全、可用性、可观测性
5. 系统架构、组件图和数据流
6. 数据模型、关系和约束
7. 凭据威胁模型及录入、更新、清除流程
8. 分发与目标平台设计
9. 技术选型及理由
10. 每项功能的客观验收标准
11. 风险、未决问题和明确不做的内容

### 凭据设计

如果使用 LLM 或其他鉴权 API：

- 不在源码、日志、Git 历史和命令行中保存真实 key。
- 首次运行使用隐藏输入。
- 支持查看状态、更新和清除，但永不回显明文。
- 优先使用操作系统钥匙串或带主密码的加密文件。
- `.env` 只能作为有明确风险说明的备选，并加入 `.gitignore`。

如果项目不需要任何外部凭据，在 SPEC 中明确写明“不使用外部鉴权 API”，并说明对应安全边界。

### Gate 2

逐条检查用户故事、功能规约和验收标准之间是否可追踪；任何“智能”“友好”“快速”等模糊词都必须变成可测试指标。

---

## 阶段 4：使用 writing-plans 完成 `PLAN.md`

### 每个 Task 必须包含

- Task 编号和目标
- 前置依赖
- 涉及的准确文件路径
- 实现要点
- 首先编写的失败测试
- 运行测试和验证结果的命令
- 完成标准
- 是否可以与其他 Task 并行

按照课程要求，将步骤细化为约 2–5 分钟的可执行动作；一个 Task 应能由一个新鲜 subagent 在一次会话内完成。

### 推荐依赖顺序

1. 测试与项目骨架
2. 数据模型和存储层
3. 核心领域逻辑
4. API / 服务层
5. 第一个端到端纵向切片
6. 其余功能模块
7. CLI，以及可选的 WebUI
8. 凭据管理
9. 可观测性和错误处理
10. 分发、CI 和部署

### Gate 3

确认每个验收标准都至少映射到一个 Task 和一个测试。

---

## 阶段 5：陌生智能体冷启动验证

### 操作要求

1. 使用与主开发智能体不同类型的智能体。
2. 开启全新会话，不导入历史、memory 或口头背景。
3. 只提供 `SPEC.md` 和 `PLAN.md`。
4. 让它在隔离的临时分支或 worktree 中尝试 1–2 个 Task。
5. 明确要求遇到不确定之处立即暂停提问，不得自行猜测。
6. 冷启动代码只作为规约验证证据，修订完成前不合入正式开发分支。

### 在 `SPEC_PROCESS.md` 记录

- 它在哪些地方暂停
- 它提出了哪些问题
- 哪些解读偏离了原意
- 偏差来自规约缺陷还是阅读错误
- 规约修订前后的关键 diff
- 修订后的验收标准是否更明确

### Gate 4

冷启动暴露的关键歧义全部修订，并重新检查 SPEC 与 PLAN 一致性后，才开始正式实现。

---

## 阶段 6：按 Task 进行 TDD 和两阶段评审

每个 Task 都严格执行以下循环：

1. 创建独立分支或 worktree。
2. 派发一个新鲜 subagent，只提供该 Task 所需上下文。
3. **Red**：先写失败测试并保存失败输出。
4. **Green**：编写使测试通过的最少实现。
5. **Refactor**：在测试保护下整理设计。
6. 运行相关测试、全量测试、lint 和类型检查。
7. **评审一：Spec 合规检查**，确认没有缺项或擅自扩展。
8. 修复所有合规问题。
9. **评审二：代码质量检查**，检查安全、可维护性和测试质量。
10. 修复所有 Critical issue。
11. 提交 commit，并在 message 或 MR/PR 描述中标注 subagent 与人工修改。
12. 更新 `PLAN.md` 的完成状态和 commit hash。
13. 更新 `AGENT_LOG.md`。

### 每个 Task 的证据

- 失败测试结果
- 通过测试结果
- 两阶段评审结论
- commit hash
- 人工修改及原因

---

## 阶段 7：集成与端到端验收

### 必测内容

- 三个以上核心模块的单元测试
- 模块边界的集成测试
- 至少一条完整用户主流程的端到端测试
- 错误输入、空数据、重复操作和外部服务失败
- 凭据缺失、错误、更新和清除流程
- 权限边界与敏感信息脱敏

### 一键测试

提供一个统一命令，例如：

```text
make test
```

该命令必须能在全新环境中运行核心测试，且单元测试不依赖真实付费 API。

---

## 阶段 8：CI、分发和部署

### CI

- 创建 `.gitlab-ci.yml`。
- 必须包含名为 `unit-test` 的 job。
- 每次 push 自动执行测试。
- 根据技术栈增加 lint、类型检查和构建任务。
- 最后一次 CI/CD 必须通过。

### 分发

首先选择并完成助教认可的一种交付方案：

#### 方案一：仅 CLI + Release

- 构建可直接获取和运行的 CLI 分发产物。
- 在 GitHub 等托管平台创建带版本号的 Release。
- Release 页面提供产物、版本说明、目标平台与 CPU 架构、安装和运行方法。
- 在最终提交材料和 README 中提供 Release 链接。

#### 方案二：CLI + WebUI

- 通过 Release、镜像仓库或包管理器提供 CLI 分发产物。
- 部署一个截止检查时仍可访问的 WebUI。
- README 写明 WebUI 公网 URL、部署架构和 CI/CD 流程。
- 为 CLI 与服务端尽量复用同一核心模块，避免维护两套业务逻辑。

CLI 分发产物可采用以下一种或多种形式：

- Docker / OCI 容器
- 原生可执行文件
- npm、PyPI、cargo 等软件包

README 必须给出获取、安装、运行、目标机器上的凭据配置和已知限制。

### WebUI 和线上部署（仅方案二）

- 设置调用额度、超时和错误降级，避免产生意外费用。
- 使用部署平台的 Secret 管理，不把生产凭据写入仓库或镜像。

---

## 阶段 9：完成最终文档

### `README.md`

至少包含：

- 项目简介
- 安装
- 运行
- 测试
- 分发命令
- Release / 分发链接；方案二还需提供 WebUI 部署 URL
- 目录结构
- API Key 安全配置
- 安全边界
- 已知限制
- 第三方依赖与许可证

### `AGENT_LOG.md`

按时间顺序记录：

- 时间戳和 Task 编号
- 使用的 Superpowers 技能
- 关键 prompt / context
- subagent 输出或 commit hash
- 人工干预及理由
- 学到的教训

### `REFLECTION.md`

由学生本人完成 1500–2500 字反思。AI 只能辅助润色且需要标注，不能代写。

---

## 阶段 10：提交前审计

### 功能和测试

- [ ] 三个以上职责清晰的功能模块
- [ ] 核心功能测试、集成测试和端到端测试通过
- [ ] 一键测试命令可用
- [ ] 已选择“仅 CLI + Release”或“CLI + WebUI”
- [ ] 方案一的 Release 链接可访问，且分发产物可下载和运行
- [ ] 方案二的 CLI 分发链接或命令有效
- [ ] 若选择方案二，WebUI 公网地址可访问

### 过程证据

- [ ] `SPEC.md`
- [ ] `PLAN.md`，完成项带 commit hash
- [ ] `SPEC_PROCESS.md`，包含至少三轮迭代和冷启动证据
- [ ] `AGENT_LOG.md`
- [ ] 完整 commit 与 MR/PR 历史
- [ ] 每个 Task 有 TDD 和两阶段评审证据

### 安全和分发

- [ ] Git 当前版本及历史中不存在真实凭据
- [ ] 日志和错误信息不泄露凭据
- [ ] 分发产物可在全新环境运行
- [ ] README 与最终提交材料中包含所选方案要求的 Release / 分发链接
- [ ] README 说明目标机器上的安全配置方式

### CI/CD

- [ ] `.gitlab-ci.yml` 包含 `unit-test`
- [ ] 最后一次 CI/CD 为通过状态
- [ ] 构建和部署记录可查

### 最终文档

- [ ] README 必备章节齐全
- [ ] `REFLECTION.md` 为本人撰写且字数合规
- [ ] 线上 URL、运行命令和已知限制准确

全部检查通过后再提交仓库链接。
